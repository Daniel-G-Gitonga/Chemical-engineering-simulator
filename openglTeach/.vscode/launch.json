{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Launch MyProject",
        "type": "cppdbg",
        "request": "launch",
        "program": "${workspaceFolder}/build/revOp.exe",
        "args": [],
        "stopAtEntry": false,
        "cwd": "${workspaceFolder}/build",
        "environment": [],
        "externalConsole": true,
        "MIMode": "gdb",   // or "lldb" if using Clang, or "gdb" with MinGW
        "miDebuggerPath": "gdb", // or "lldb" or your debugger path
        "setupCommands": [
          { "description": "Enable pretty-printing", "text": "-enable-pretty-printing", "ignoreFailures": true }
        ]
      }
    ]
  }